# AirBnb console Clone project with python

### Clone project

In the project directory,in your terminal, you can run:

```cmd
 git clone https://github.com/Ginohmk/portfolio-v2.git
```

```cmd
  cd portfolio-v2
```
